package com.workshop.test;

import java.util.List;

import com.workshop.constants.BotType;
import com.workshop.constants.UserChoice;
import com.workshop.model.Player;
import com.workshop.operations.GameRule;

public class GameRuleTest {

	/*
	 * public static void main1(String args[]) { GameRule gameRule = new GameRule();
	 * List<Integer> scores = gameRule.getScoreBasedOnRule(new Player("sam"), new
	 * Player("sam")); System.out.println(scores.toString()); }
	 * 
	 * public static void main2(String args[]) { GameRule gameRule = new GameRule();
	 * List<Integer> scores = gameRule.getScoreBasedOnRule(new
	 * Player(BotType.EVIL.name(), true), new Player("sam"));
	 * System.out.println(scores.toString()); }
	 * 
	 * public static void main3(String args[]) { GameRule gameRule = new GameRule();
	 * List<Integer> scores = gameRule.getScoreBasedOnRule(new Player("sam"), new
	 * Player(BotType.EVIL.name(), true)); System.out.println(scores.toString()); }
	 * 
	 * public static void main(String args[]) { GameRule gameRule = new GameRule();
	 * List<Integer> scores = gameRule.getScoreBasedOnRule(new
	 * Player(BotType.EVIL.name(), true), new Player(BotType.EVIL.name(), true));
	 * System.out.println(scores.toString()); }
	 */
}
